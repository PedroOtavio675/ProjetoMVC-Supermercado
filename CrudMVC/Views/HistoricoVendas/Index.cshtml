@model List<VendaModel>

<h1>Historico de vendas</h1>
@if(Model != null)
{
    @foreach (VendaModel venda in Model)
    {
        
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Codigo da venda</th>
                <th scope="col">ID do produto</th>
                <th scope="col">Preço Uni R$</th>
                <th scope="col">Total R$</th>
                <th scope="col">Quantidade</th>
                <th scope="col">Data</th>
                <th scope="col">Remover</th>
            </tr>
        </thead>
        <tr> 
            @* <th scope="row">@venda.Id</th>
            <td>@venda.</td>
            <td>@venda.PrecoUnitario</td>
            <td>@(venda.ValorTotal / 100)</td>
            <td>@venda.Quantidade</td>
            <td>@venda.Data</td> *@
            <td>
                <div class="btn-group">
                        <a type="button" class="btn btn-danger" asp-route-id="@venda.Id" asp-action="ExcluirConfirma" asp-controller="HistoricoVendas">Remover</a>
                </div>
            </td>
        </tr>
        </table>
    }
}
else
{
    <h1>Deu bosta</h1>
}

